# shell-dex agents

Put all AI-agent-specific materials for `shell-dex` in this directory.

Examples:

- project context and knowledge files
- prompts and role definitions
- review playbooks
- session artifacts that belong to the `shell-dex` repository itself

Do not scatter agent-specific files across the application root.
